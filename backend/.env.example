# =============================================================================
# BNI PALMS Analytics - Backend Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit the .env file to version control!
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# Django Core Settings
# -----------------------------------------------------------------------------

# SECRET_KEY - Django secret key for cryptographic signing
# SECURITY: Generate a new secret key for production!
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION

# DEBUG - Enable/disable debug mode
# SECURITY: MUST be False in production!
# Values: "True" or "False" (case-sensitive)
DEBUG=True

# ALLOWED_HOSTS - Comma-separated list of allowed hostnames
# Examples:
#   Development: localhost,127.0.0.1
#   Production: yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# VERCEL - Set to "1" if deploying to Vercel (automatically adds Vercel hosts)
# Leave empty for non-Vercel deployments
# VERCEL=1

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

# DATABASE_URL - PostgreSQL connection string (for production)
# Format: postgresql://user:password@host:port/database
#
# Examples:
#   Local PostgreSQL: postgresql://postgres:password@localhost:5432/bni_palms
#   Supabase: postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres
#
# If not set, SQLite will be used for development (db.sqlite3)
# DATABASE_URL=postgresql://user:password@host:port/database

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------

# CORS_ALLOWED_ORIGINS - Comma-separated list of allowed frontend origins
# Must include protocol (http:// or https://)
# Examples:
#   Development: http://localhost:3000,http://127.0.0.1:3000
#   Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# Optional: Additional Configuration
# -----------------------------------------------------------------------------

# TIME_ZONE - Django timezone setting (default: UTC)
# Example: Asia/Dubai
# TIME_ZONE=UTC

# LANGUAGE_CODE - Django language code (default: en-us)
# LANGUAGE_CODE=en-us

# -----------------------------------------------------------------------------
# Production Checklist
# -----------------------------------------------------------------------------
# Before deploying to production, ensure:
#
# ✅ SECRET_KEY is changed to a new random value
# ✅ DEBUG is set to False
# ✅ ALLOWED_HOSTS includes your production domain
# ✅ DATABASE_URL points to production database (PostgreSQL/Supabase)
# ✅ CORS_ALLOWED_ORIGINS includes your production frontend URL
# ✅ HTTPS is configured (required for security headers)
# ✅ Environment variables are set in hosting platform (Vercel/Railway/etc)
#
# Security Headers:
# ✅ HSTS, CSP, and other security headers are automatically enabled when DEBUG=False
# ✅ No additional environment variables needed for security headers
# =============================================================================
